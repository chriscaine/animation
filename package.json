{
  "name": "animation-server",
  "version": "1.0.0",
  "description": "Server watches a folder for new images and pushes the images out on a socket to the client view for previewing and controlling rendering of video on server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Caine",
  "license": "ISC",
  "dependencies": {
    "node-watch": "^0.5.5",
    "rxjs": "^5.4.2",
    "serve-static": "^1.12.3",
    "socket.io": "^1.3.7",
    "express": "^4.10.2",
    "fluent-ffmpeg": "^2.0.1"
  },
  "devDependencies": {
    "request": "^2.88.0"
  }
}
